{
  "name": "osm-navigator-monorepo",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "yarn workspace @osm-navigator/core build && yarn workspace @osm-navigator/native-map build && yarn workspace @osm-navigator/ui-navigation build",
    "typecheck": "yarn workspace @osm-navigator/core typecheck && yarn workspace @osm-navigator/native-map typecheck && yarn workspace @osm-navigator/ui-navigation typecheck",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "@osm-navigator/native-map": "*",
    "typescript": "^5.4.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0"
  },
  "expo": {
    "autolinking": {
      "searchPaths": [
        "packages"
      ]
    }
  }
}
